import { InformacionUsuario } from './informacion-usuario';

describe('InformacionUsuario', () => {
  it('should create an instance', () => {
    expect(new InformacionUsuario()).toBeTruthy();
  });
});
